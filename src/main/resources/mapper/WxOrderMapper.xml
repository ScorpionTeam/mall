<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scoprion.mall.littlesoft.mapper.WxOrderMapper">
    <select id="findAll" resultType="com.scoprion.mall.domain.Order">
        SELECT
        id,
        orderNo,
        goodSnapShotId,
        deliveryId,
        orderStatus,
        orderType,
        createDate,
        updateDate,
        userId
        FROM t_order
        WHERE
        userId=#{userId} AND orderStatus=#{orderStatus}
    </select>
</mapper>