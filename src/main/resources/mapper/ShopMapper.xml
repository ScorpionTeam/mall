<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scoprion.mall.backstage.mapper.ShopMapper">

    <!--商铺列表 AUDITING 待审核/审核中    NOT_PASS_AUDIT审核未通过  PASS_AUDIT审核通过-->
    <select id="findPage" resultType="com.scoprion.mall.domain.Seller">
        SELECT
        id,
        audit,
        status,
        mobile,
        user_id,
        seller_no,
        reputation,
        seller_name,
        seller_address,
        seller_description
        FROM t_seller WHERE 1=1
        <if test="audit!=null">AND audit=#{audit}</if>
        <if test="searchKey!=null">AND CONCAT_WS('',seller_no,seller_name,seller_address,seller_description)
            LIKE #{searchKey}
        </if>
    </select>

    <!--店铺审核-->
    <update id="update" parameterType="com.scoprion.mall.domain.Seller">
        UPDATE t_seller SET
        <if test="audit!=null and  audit!='' ">audit=#{audit},</if>
        <if test="reason!=null and  reason!=''">reason=#{reason},</if>
        status='NORMAL',
        update_date=now()
        WHERE id=#{id}
    </update>

    <!--商铺详情-->
    <select id="findById" resultType="com.scoprion.mall.domain.Seller">
        SELECT
        id,
        audit,
        status,
        mobile,
        user_id,
        seller_no,
        reputation,
        seller_name,
        seller_address,
        seller_description
        FROM t_seller
        WHERE id=#{id}
    </select>
</mapper>