<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scoprion.mall.seller.mapper.SellerMapper">
    <!--新增商铺-->
    <insert id="add" parameterType="com.scoprion.mall.domain.Seller">
        INSERT INTO t_seller(
        audit,
        status,
        mobile,
        user_id,
        seller_no,
        reputation,
        seller_name,
        seller_address,
        seller_description,
        create_date)
        VALUES (
        'AUDITING',
        'NORMAL',
        #{mobile},
        #{userId},
        #{sellerNo},
        '50',
        #{sellerName},
        #{sellerAddress},
        #{sellerDescription},
        NOW()
        )
    </insert>

    <!--删除店铺-->
    <delete id="delete">
        DELETE FROM t_seller WHERE id=#{id}
    </delete>

    <!--修改店铺信息-->
    <update id="modify" parameterType="com.scoprion.mall.domain.Seller">
        UPDATE t_seller SET
        <if test="mobile!=null">mobile=#{mobile},</if>
        <if test="sellerName!=null">seller_name=#{sellerName},</if>
        <if test="sellerAddress!=null">seller_address=#{sellerAddress},</if>
        <if test="sellerDescription!=null">seller_description=#{sellerDescription},</if>
        update_date=now()
        WHERE id=#{id}
    </update>

    <!--校验名称是否存在-->
    <select id="validByName" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_seller WHERE seller_name=#{sellerName} AND status='NORMAL'
    </select>

    <!--校验商户是否已经存在商铺-->
    <select id="validByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_seller WHERE user_id=#{userId} AND status='NORMAL'
    </select>

    <!--校验号码是否存在-->
    <select id="validByMobile" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_user WHERE mobile=#{mobile}
    </select>

    <!--校验昵称是否存在-->
    <select id="validByNickName" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_user WHERE nick_name=#{nickName}
    </select>

    <!--修改商户个人信息-->
    <update id="alter" parameterType="com.scoprion.mall.domain.MallUser">
        UPDATE t_user SET
        <if test="headPic!=null">head_picture=#{headPic},</if>
        <if test="nickName!=null">nick_name=#{nickName},</if>
        <if test="city!=null">city=#{city},</if>
        <if test="mobile!=null">mobile=#{mobile},</if>
        <if test="email!=null">email=#{email},</if>
        <if test="address!=null">address=#{address},</if>
        update_date=now()
        WHERE id=#{id}
    </update>

    <!--微信商户登录(手机号)-->
    <select id="login" resultType="com.scoprion.mall.domain.MallUser">
        SELECT id,
        name,
        mobile,
        born_date,
        sex,
        age,
        email,
        address,
        password,
        head_picture,
        nick_name,
        certification,
        certificate_id,
        city,
        status,
        create_date,
        update_date,
        last_login_date,
        login_ip,
        user_type,
        id_photo_front_url,
        id_photo_bg_url
        FROM t_user WHERE status='NORMAL'
        AND(email=#{email} AND password=#{password})
        OR(mobile=#{mobile} AND password=#{password})
    </select>

    <!--更新商户最后登录的ip地址-->
    <update id="updateLoginIpAddress" parameterType="com.scoprion.mall.domain.MallUser">
        UPDATE t_user SET
        login_ip=#{ip}
        WHERE id=#{id}
    </update>

    <!--商户注册-->
    <insert id="register" parameterType="com.scoprion.mall.domain.MallUser">
        <selectKey resultType="java.lang.Long" keyProperty="id" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO t_user(
        name,
        mobile,
        born_date,
        sex,
        age,
        email,
        address,
        password,
        head_picture,
        nick_name,
        certification,
        certificate_id,
        city,
        status,
        create_date,
        user_type,
        id_photo_front_url,
        id_photo_bg_url)
        VALUES (
        #{name},
        #{mobile},
        #{bornDate},
        #{sex},
        #{age},
        #{email},
        #{address},
        #{password},
        #{headPic},
        #{nickName},
        'NOT_AUTH',
        #{certificateId},
        #{city},
        'NORMAL',
        NOW(),
        'SELLER',
        #{idPhotoFrontUrl},
        #{idPhotoBgUrl}
        )
    </insert>

    <!--商户订单列表-->
    <select id="findByUserId" resultType="com.scoprion.mall.domain.Seller">
        SELECT
        id,
        audit,
        status,
        mobile,
        user_id,
        seller_no,
        reputation,
        seller_name,
        seller_address,
        seller_description
        FROM t_seller WHERE  user_id=#{userId} AND status='NORMAL'
    </select>

    <select id="validCertification" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM t_user
        WHERE id=#{userId} AND status='NORMAL' AND certification='IS_AUTH'
    </select>
</mapper>