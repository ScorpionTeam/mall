<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scoprion.mall.mapper.DeliveryMapper">
    <insert id="add" parameterType="com.scoprion.mall.domain.Delivery">
        INSERT INTO t_delivery(userId,
        recipients,
        phone,
        province,
        city,
        area,
        address,
        postCode,
        createDate,
        updateDate)
        VALUES (#{userId},
        #{recipients},
        #{phone},
        #{province},
        #{city},
        #{area},
        #{address},
        #{postCode},
        NOW(),
        NOW())
    </insert>
</mapper>