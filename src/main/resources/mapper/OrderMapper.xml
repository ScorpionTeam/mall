<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scoprion.mall.mapper.OrderMapper">

    <insert id="add" parameterType="com.scoprion.mall.domain.Order">
        INSERT INTO t_order(orderNo,
        goodSnapShotId,
        deliveryId,
        orderStatus,
        orderType,
        createDate,
        updateDate)
        VALUES (#{orderNo},
        #{goodSnapShotId},
        #{deliveryId},
        #{orderStatus},
        #{orderType},
        NOW(),
        NOW())
    </insert>
</mapper>